import React from "react"
import Skill from "../../Profile/Skills/Skill/Skill";

const EventsAll = () => {
    // let skillsItem =
    //     props.skills.map(p => <Skill key={p.id} icon={p.icon} name={p.name}/>);
    //

}

export default EventsAll;